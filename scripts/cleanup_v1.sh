# remove all files in models/
rm -f models/*

# remove all files in self-play-games
rm -f self-play-games/*

# remove all data in training-metrics
rm -f training-metrics/*

# remove all logs
# rm -f logs/*